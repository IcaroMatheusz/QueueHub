/* eslint-disable prettier/prettier */
const titulo: string = "Computador não liga";

const prioridade: number = 1;

const resolvido: boolean = false;

console.log(titulo);
console.log(prioridade);
console.log(resolvido);